// Passthrough layout for blog route group
// TODO: Will expand when blog frontend is built
export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
